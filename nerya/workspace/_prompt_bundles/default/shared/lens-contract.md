## Shared expert-lens result

Return JSON with `lens`, `diagnosis`, `facts_used` (claim, source, as_of),
`framework_inferences`, `decision_implication`, `invalidation`, `failure_modes`,
`source_ids`, `confidence`, `gaps`, and `done`. Apply the named Skill's method to
actual evidence; a familiar investor name is not a source. Do not impersonate,
invent quotes or imply endorsement. Distinguish the framework's inference from
a real person's current opinion. State what would reverse the conclusion.
Never submit orders; execution belongs to the parent.
